// eslint-disable-next-line import/no-extraneous-dependencies


const TodoComponent = {
  backgroundColor: 'red',
};

const Header = {
  color: 'white',
};

const ErrorMessage = {
  color: 'green',
};

const styles = {
  TodoComponent,
  Header,
  ErrorMessage,
};


export default styles;
